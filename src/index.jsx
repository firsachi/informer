import React from 'react';
import ReactDOM from 'react-dom/client';
import LayoutEmployee from './components/LayoutEmployee';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <LayoutEmployee />
  </React.StrictMode>
);